#!/bin/bash

__dirname="$( cd "$( dirname "${BASH_SOURCE[0]}" )" >/dev/null 2>&1 && pwd )"

touch ~/.bashrc
rm ~/.bash_aliases

echo "\n. $__dirname/init.sh" >> ~/.bashrc
ln -s "$__dirname/init.sh" ~/.bashrc
